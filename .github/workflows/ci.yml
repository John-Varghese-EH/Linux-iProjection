name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  lint-and-test:
    runs-on: ubuntu-24.04
    
    steps:
    - uses: actions/checkout@v5

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y python3-gi python3-gi-cairo gir1.2-gtk-4.0 libadwaita-1-dev gir1.2-gstreamer-1.0 gir1.2-gst-plugins-base-1.0 gstreamer1.0-plugins-base gstreamer1.0-plugins-good python3-pytest python3-pip python3-venv
        
    - name: Install Python dependencies
      run: |
        python3 -m venv .venv --system-site-packages
        .venv/bin/pip install --upgrade pip
        .venv/bin/pip install -e ".[dev]"
        
    - name: Check code formatting and linting
      run: .venv/bin/ruff check src/ tests/
        
    - name: Run test suite
      run: .venv/bin/python -m pytest tests/ -v
