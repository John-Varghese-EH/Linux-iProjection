# linux-iprojection — Python Requirements
# =========================================
#
# ✅ WINDOWS-SAFE (pip install works on all platforms)
#    Install with:  pip install -r requirements.txt
#
# ❌ LINUX-ONLY (system packages, NOT pip)
#    Install with:  ./scripts/setup_linux_dev.sh
#    These are installed automatically on Linux by the setup script:
#      python3-gi, gir1.2-gtk-4.0, gir1.2-adw-1,
#      gir1.2-gstreamer-1.0, python3-gst-1.0,
#      gstreamer1.0-pipewire, pipewire, wireplumber,
#      xdg-desktop-portal, xdg-desktop-portal-gnome

# ── Network discovery (Windows + Linux) ──────────────────────────────────────
zeroconf>=0.128.0          # mDNS / DNS-SD (python-zeroconf)
netifaces2>=0.0.19         # Pure-Python drop-in replacement for netifaces (Windows + Linux)

# ── Device control (Windows + Linux) ─────────────────────────────────────────
# (ESC/VP.net and PJLink use asyncio TCP — no extra deps)

# ── DBus / Portal (Linux only, but pip-installable stub is safe on Windows) ──
dasbus>=1.7                # xdg-desktop-portal DBus client (used only on Linux)

# ── Dev / test ────────────────────────────────────────────────────────────────
pytest>=8.1
pytest-asyncio>=0.23
